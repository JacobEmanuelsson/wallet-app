node_modules
# Keep environment variables out of version control
.env
!.env.example

/src/generated/prisma

# AI coding-assistant skill docs (auto-fetched by `prisma init` / `prisma skills sync`)
.agents/
.claude/
.windsurf/
skills-lock.json
